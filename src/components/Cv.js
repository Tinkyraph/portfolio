import React from 'react';

const Cv = () => {
    return (
        <div>
            
        </div>
    );
};

export default Cv;
{/*importer le cv (voir si recréer un n'est pas necessaire (charte graphique lié au portfolio*/}